## Техническое задание (спецификация) для агента на Flutter + BLoC

**Проект:** «Агент с двухпотоковой рефлексией и временными ограничениями»  
**Платформы:** Android, iOS, Web (основные), десктоп (опционально)  
**Фреймворк:** Flutter (3.x)  
**Управление состоянием:** BLoC (Cubit или полноценный BLoC)  
**Архитектура:** Clean Architecture (слои: Presentation → Domain → Data) + внедрение зависимостей (get_it)

---

### 1. Основные технологии и пакеты (Flutter)

| Назначение | Пакет (pub.dev) |
|------------|------------------|
| BLoC | `flutter_bloc` + `equatable` |
| HTTP-запросы к LLM API | `dio` (с поддержкой отмены запросов) |
| Асинхронность и таймеры | `dart:async` (Stream) |
| Внедрение зависимостей | `get_it` |

---

### 2. Модели данных (Domain layer)

```dart
// lib/domain/entities/agent_role.dart
enum AgentRole { generator, critic }

// lib/domain/entities/stopped_reason.dart
enum StoppedReason { timeout, noIssues, userCancelled, maxIterations, noImprovement }

// lib/domain/entities/internal_step.dart
class InternalStep {
  final AgentRole role;
  final String content;
  final DateTime timestamp;
  final int iteration;
  InternalStep({required this.role, required this.content, required this.timestamp, required this.iteration});
}

// lib/domain/entities/final_answer.dart
class FinalAnswer {
  final String text;
  final List<InternalStep> internalSteps;
  final Duration totalTime;
  final int iterationsUsed;
  final StoppedReason stoppedReason;
  FinalAnswer({required this.text, required this.internalSteps, required this.totalTime, required this.iterationsUsed, required this.stoppedReason});
}
```

---

### 3. BLoC-схема (Presentation layer)

**ChatBloc** управляет процессом рефлексии, подписываясь на поток от UseCase.

#### 3.1. Стейты (`chat_state.dart`)

```dart
class ChatReflecting extends ChatState {
  final int currentIteration;
  final Duration remainingTime;
  final String? lastUpdate;
  final AgentRole? lastRole;
  final List<InternalStep> steps;
  // ... copyWith
}
```

---

### 4. Use Case (Domain layer): `ReflectAgentUseCase`

Ядро логики, реализующее двухпотоковую рефлексию. Возвращает `Stream<ReflectionStep>`.

```dart
class ReflectAgentUseCase {
  final LlmRepository llmRepository;

  Stream<ReflectionStep> execute({
    required String query,
    required TimePreset timePreset,
    Object? cancelToken,
  }) async* {
    // 1. Первая генерация (Generator)
    // 2. Цикл:
    //    - Критика (Critic) -> yield ReflectionStep
    //    - Если "NO_ISSUES" -> break
    //    - Исправление (Generator) -> yield ReflectionStep
    //    - Проверка лимитов времени и итераций
  }
}
```

---

### 5. Репозиторий (Domain/Data layer)

```dart
abstract class LlmRepository {
  Future<String> generate({
    required String systemPrompt,
    required List<ChatMessage> messages,
    Object? cancelToken,
  });
}
```

---

### 6. Итоговая реализация (Статус)

- [x] Спроектирован Domain Layer (Entities, UseCases).
- [x] Реализован Data Layer (DioLlmRepository для DeepSeek/OpenAI).
- [x] Реализован Presentation Layer (ChatBloc, ChatScreen).
- [x] Настроен DI (GetIt).
- [x] Добавлена визуализация процесса «раздумий» (Таймер, итерации, логи).

---

## Резюме идеи

Система использует **два потока промптов** (Генератор и Критик) для итеративного улучшения ответа в рамках заданного пользователем **временного бюджета** (`TimePreset`). Это позволяет получать качественные ответы даже от менее мощных (в том числе бесплатных) моделей за счет рефлексии.
